/* Everything personal lives in one file. This re-export exists so the
 * components that already import `profile` keep working — edit
 * content/personal.ts, not this. */
export { profile, tagline } from "./personal";
